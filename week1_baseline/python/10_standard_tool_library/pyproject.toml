[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "boukensha"
version = "0.10.0"
description = "BOUKENSHA — a tiny teaching framework for coding harnesses"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Andrew Brown", email = "andrew@exampro.co"}]
dependencies = [
    "python-dotenv",
    "PyYAML",
    "requests",
]

[project.scripts]
boukensha = "boukensha_loader:main"

[tool.setuptools]
package-dir = {"" = "lib"}
packages = ["boukensha", "boukensha.tasks", "boukensha.backends", "boukensha.tools"]
py-modules = ["boukensha_loader"]
